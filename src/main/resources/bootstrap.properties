#name as appeared in config server config files
spring.application.name=service-B
#address of our config server
spring.cloud.config.uri=http://localhost:9999
#we will write a port, and we will be expected that the port will be changed to 8001
server.port=0
